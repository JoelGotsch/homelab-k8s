# GatewayClass — references Cilium's controller.
# Cilium installs the Gateway API CRDs as part of its chart
# (gatewayAPI.enabled: true in cilium values).

apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: cilium
spec:
  controllerName: io.cilium/gateway-controller
