# StorageClasses backed by csi-rclone — one per `personal+`
# share per ADR 0030 §D1. Each maps to a chained
# `crypt:` over `nfs:` remote whose config sits in the matching
# Secret (see externalsecret.yaml).
#
# Adding a new share at consumer-onboarding time:
#   1. Operator generates the per-share crypt key +
#      offline-recovery archive update per
#      `03-runbooks/nas/rclone-crypt-key-bootstrap.md`.
#   2. Add an ExternalSecret in this layer pulling
#      `kv/prod/nas-encryption/<share>/rclone_config`.
#   3. Add the matching StorageClass below pointing at the
#      Secret name.
#   4. Cold-start.md Step 13c gets a row for the new share.

# nas-crypt-personal-photos — Immich library backend.
# Per ADR 0030 §D2 only the *originals* sub-tree of Immich's
# UPLOAD_LOCATION lives here; thumbs/encoded-video/profile
# stay on Longhorn.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-personal-photos
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  # Top-level remote name as defined in the rclone INI in the
  # referenced Secret. Driver mounts `<remote>:<remotePath>`.
  remote: "nas-crypt-personal-photos"
  remotePath: ""

  # Secret holding the rclone INI (configData key). Per-share
  # so that key compromise of one share doesn't expose
  # another.
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-personal-photos-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"

  # Per-share mount options override the driver-level defaults
  # in values.yaml when the workload calls for it. Immich's
  # cold-originals access pattern is well-served by the
  # driver-level defaults; left empty here.
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-personal-files — Nextcloud's primary data dir.
# Per ADR 0030 §D2 only the user-files tree lives here;
# Nextcloud's config / custom_apps / themes / tmp + DB cache
# stay on Longhorn.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-personal-files
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-personal-files"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-personal-files-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-family-shared — Nextcloud Group Folder for
# family-shared content (cross-account, non-private).
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-family-shared
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-family-shared"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-family-shared-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-internal-archive — Nextcloud Group Folder for
# archived internal documents (operator-driven, non-active).
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-internal-archive
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-internal-archive"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-internal-archive-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-forgejo-lfs — Forgejo Git LFS objects per
# ADR 0023 D12 + ADR 0019 D7. WORM-ish workload (LFS objects
# are content-addressed; once written, never modified).
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-forgejo-lfs
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-forgejo-lfs"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-forgejo-lfs-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-registry-blobs — Forgejo Packages (artifact
# registry) blob storage per ADR 0019 D7. WORM-ish workload
# (OCI / PyPI / Helm artefacts are content-addressed).
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-registry-blobs
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-registry-blobs"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-registry-blobs-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
---
# nas-crypt-personal-documents — Paperless-ngx source
# documents. Per ADR 0030 §D2 only the document originals
# (and Paperless's archive variants) live here; thumbnails
# and search-index caches stay on Longhorn.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nas-crypt-personal-documents
provisioner: rclone.csi.veloxpack.io
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  remote: "nas-crypt-personal-documents"
  remotePath: ""
  csi.storage.k8s.io/node-publish-secret-name: "nas-crypt-personal-documents-config"
  csi.storage.k8s.io/node-publish-secret-namespace: "csi-rclone"
  mountOptions: "--allow-other --file-perms 0666 --dir-perms 0777"
