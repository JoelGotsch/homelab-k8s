# external-secrets Helm values.

installCRDs: true

resources:
  requests: {cpu: 50m, memory: 128Mi}
  limits: {memory: 512Mi}

webhook:
  resources:
    requests: {cpu: 25m, memory: 64Mi}
    limits: {memory: 256Mi}

certController:
  resources:
    requests: {cpu: 25m, memory: 64Mi}
    limits: {memory: 256Mi}

serviceMonitor:
  enabled: true
