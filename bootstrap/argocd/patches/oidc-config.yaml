# BEGIN ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# RENDERED FILE — DO NOT EDIT. Edit the .j2 sibling and re-run:
#   ansible-playbook ansible/playbooks/00-render-static.yml
# (from the homelab-infra repo root).
# END ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# Authentik OIDC for Argo CD operator login.
# Operator UN-COMMENTS the patches[] reference in the parent
# kustomization.yaml when Authentik is up + the argocd OIDC
# client is created. Until then, Argo CD admin password lands
# as a Secret at first install (per cold-start.md Step 13c).

apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  oidc.config: |
    name: Authentik
    issuer: https://auth.lab.vyramo.com/application/o/argocd/
    clientID: <AUTHENTIK_CLIENT_ID_ARGOCD>
    clientSecret: $oidc.authentik.clientSecret  # ESO-synced from OpenBao
    requestedScopes: ["openid", "profile", "email", "groups"]
    requestedIDTokenClaims: { groups: { essential: true } }
