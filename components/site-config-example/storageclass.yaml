# Example: a full-value field (the NAS IP) injected from site-config.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: demo-nfs
provisioner: nfs.csi.k8s.io
parameters:
  server: NAS_IP_PLACEHOLDER   # replaced with <nas_storage_ip>
  share: /volume1/demo
