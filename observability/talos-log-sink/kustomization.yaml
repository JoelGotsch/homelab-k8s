# Talos log sink — kernel + machined log receiver.
#
# Paired with homelab-infra/talos/patches/logging-destinations
# .yaml. See README.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - configmap.yaml
  - daemonset.yaml
  - service.yaml
  - networkpolicy.yaml
  - prometheusrule.yaml
