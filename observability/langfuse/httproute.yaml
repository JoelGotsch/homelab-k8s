# BEGIN ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# RENDERED FILE — DO NOT EDIT. Edit the .j2 sibling and re-run:
#   ansible-playbook ansible/playbooks/00-render-static.yml
# (from the homelab-infra repo root).
# END ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# Langfuse HTTPRoute via the Cilium Gateway.
#
# Phase 1 (today): Tailscale-only on
#   langfuse.lab.vyramo.com
# Operator-only access for tracing the homelab's own LLM
# pipeline; not exposed publicly (Langfuse holds raw prompts +
# completions classified `internal`/`personal` per ADR 0003).
#
# Phase 2: stays Tailscale-only. There is no `langfuse.vyramo.com`
# public route by design (ADR 0028 D7 — telemetry data does not
# leave the trust boundary).

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: langfuse
  namespace: langfuse
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: ingress
      sectionName: https
  hostnames:
    - "langfuse.lab.vyramo.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: langfuse-web
          port: 3000
