# PR-Agent webhook server (Deployment + Service).
# Per ADR 0029 D2 + D4.
#
# Image is built from qodo-ai/pr-agent's `gitea_app` Dockerfile
# target — no upstream-published image for that target as of
# release tracking. Recipe in README §"Image build". Renovate
# pins via the `<digest>`-shaped tag once a build lands in
# registry.homelab.internal.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pr-agent
  namespace: pr-agent
  labels:
    app.kubernetes.io/name: pr-agent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pr-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pr-agent
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pr-agent
          # OPERATOR PINS via Renovate. Image is built by the
          # `forgejo-admin/pr-agent-build` wrapper repo via
          # Woodpecker on every tag push; tag is
          # `gitea_app-<upstream-tag>`. Forgejo Packages OCI
          # namespaces images by owner and Forgejo only allows the
          # token-holder to push to its own namespace, so the
          # `ci-packages` bot pushes here (its own namespace), NOT
          # to `forgejo-admin/*`. Pull-side is fine because the
          # cluster's registry-pull Secret uses the same bot.
          image: "registry.homelab.internal/ci-packages/pr-agent:gitea_app-0.34"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            # Cap gunicorn worker count. Upstream's gunicorn_config
            # .py autoscales to nproc*2+1, which on a 16-core worker
            # becomes 33+ workers; each loads litellm/openai/tiktoken
            # + tokenizers at ~150 MiB resident, blowing past any
            # sane memory limit (OOMKilled at 1 GiB, 2026-06-11).
            #
            # The worker class is uvicorn.workers.UvicornWorker
            # (async), so the cores*2+1 formula upstream uses is
            # actually the gunicorn recommendation for SYNC workers
            # — async workers each run an asyncio event loop and
            # serve up to worker_connections=1000 concurrent
            # requests per process. Gunicorn docs say 2-4 async
            # workers total regardless of core count. pr-agent here
            # is a Forgejo PR-webhook handler making LLM calls; 2
            # workers cover liveness-probe headroom while one is
            # mid-LLM-call.
            #
            # NB: the env var name is upstream-custom — gunicorn's
            # standard WEB_CONCURRENCY is NOT read by their conf.
            - name: GUNICORN_WORKERS
              value: "2"
            # Re-home XDG/dotfile paths to /tmp. The azure-devops
            # Python lib (pulled in transitively by pr-agent's
            # multi-provider git layer) mkdirs ~/.azure-devops at
            # import time; with readOnlyRootFilesystem: true and
            # the container running as uid 1000 with HOME=/, that
            # resolves to /.azure-devops and OSError(EROFS) takes
            # the worker down before it can serve a request.
            # /tmp is mounted as a writable emptyDir below, so
            # /tmp/.azure-devops + any other ~/.cache/... paths
            # from sibling libs all land somewhere sane.
            - name: HOME
              value: /tmp
          envFrom:
            - configMapRef:
                name: pr-agent-config
            - secretRef:
                name: pr-agent-forgejo
            - secretRef:
                name: pr-agent-webhook
            - secretRef:
                name: pr-agent-llm-gateway
          # Probes are TCP, not HTTP. The gitea_app FastAPI surface
          # only mounts POST /api/v1/gitea_webhooks — there is no `/`
          # route and no health endpoint, so an HTTP GET probe gets a
          # 404 and kubelet kills the pod ~60s after boot. Upstream
          # has not added a healthcheck route as of gitea_app-0.34.
          # A TCP-connect probe verifies gunicorn is bound and is
          # the correct liveness signal for "the webhook listener is
          # up"; the webhook itself is the only real workload.
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: pr-agent
  namespace: pr-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pr-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
