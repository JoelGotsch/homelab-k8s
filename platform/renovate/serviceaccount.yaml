# Dedicated SA for the Renovate CronJob. No cluster-wide RBAC
# needed — Renovate just exec's against Forgejo's API and
# doesn't read or mutate kube state.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: renovate
  namespace: renovate
automountServiceAccountToken: false
