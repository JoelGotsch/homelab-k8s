# BEGIN ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# RENDERED FILE — DO NOT EDIT. Edit the .j2 sibling and re-run:
#   ansible-playbook ansible/playbooks/00-render-static.yml
# (from the homelab-infra repo root).
# END ANSIBLE-RENDERED HEADER (auto-managed; do not edit)
# Authentik blueprint — Immich OIDC client.
#
# Provisions an OAuth2Provider + Application so Immich can
# delegate authn to Authentik via its built-in OAuth2 settings.
#
# Redirect URI points at the INTERNAL Immich hostname
# (immich.lab.vyramo.com) — not `photos.vyramo.com`, which is
# the public-album proxy (immich-public-proxy) and does not
# handle authn. Confirmed against apps/immich/httproute.yaml +
# values.yaml.
#
# client_id + client_secret come from OpenBao via the
# authentik-oidc-clients ExternalSecret (kv/immich/oidc),
# projected into the worker pod as env vars by global.envFrom.
# Naming convention: AUTHENTIK_BOOTSTRAP_<APP>_CLIENT_{ID,SECRET}
# matches upstream blueprint examples + makes the env source
# unambiguous (these vars are read by !Env tags below, NOT by
# Authentik itself).
#
# Same client_id/client_secret pair is also read by Immich in
# the immich ns via a separate ExternalSecret pointing at the
# same OpenBao path. Both pull from the same source-of-truth
# so rotation only touches OpenBao.

version: 1
metadata:
  name: immich-oidc
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  # OAuth2Provider — issues tokens, defines redirect URI +
  # signing key + scopes Immich will request.
  - model: authentik_providers_oauth2.oauth2provider
    id: immich-provider
    identifiers:
      name: immich
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: !Env AUTHENTIK_BOOTSTRAP_IMMICH_CLIENT_ID
      client_secret: !Env AUTHENTIK_BOOTSTRAP_IMMICH_CLIENT_SECRET
      redirect_uris:
        - matching_mode: strict
          url: "https://immich.lab.vyramo.com/auth/login"
        # Immich MOBILE app OAuth callback (custom scheme; the app opens
        # the system browser and returns via this deep link). Without it
        # phone logins die at Authentik with a redirect_uri mismatch —
        # added 2026-07-31 for the Samsung backup phone.
        - matching_mode: strict
          url: "app.immich:///oauth-callback"
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]

  # Application — the user-facing tile in Authentik's launcher,
  # bound to the provider above.
  - model: authentik_core.application
    id: immich-app
    identifiers:
      slug: immich
    attrs:
      name: Immich
      provider: !KeyOf immich-provider
      meta_launch_url: "https://immich.lab.vyramo.com/"
      meta_description: "Photo + video library"
      group: "Media"
      policy_engine_mode: any

  # Access control — restrict this application to the listed group(s).
  # GitOps-tracks what was previously UI-only: without these, a blueprint
  # rebuild silently reopens every app to ANY authenticated user (incl.
  # family-members) — a regression that would pass unnoticed. Matches the live
  # bindings (2026-07-17): all apps operator-only. To grant family access to an
  # app, set `access_groups: [operator, family-members]` on its oidc-apps.yml
  # entry (defaults to [operator]). NB: akadmin (bootstrap superuser) is
  # intentionally NOT in `operator`, so it stays break-glass-only.
  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf immich-app
      group: !Find [authentik_core.group, [name, operator]]
    attrs:
      order: 10
      enabled: true
      negate: false
  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf immich-app
      group: !Find [authentik_core.group, [name, family-members]]
    attrs:
      order: 20
      enabled: true
      negate: false
